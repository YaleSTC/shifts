<% title "#{@role.name.pluralize}" %>


<table class = "tablesorter">
 <thead>
	<tr>
		<th> Login </th>
		<th> Name </th>
		<th> Email </th>
	</tr>
  </thead>
	<% for user in @role_users %>
  <tbody>
	<tr> 
	    <td><%= user.login%></td>
		<td><%= link_to user.name, user_profile_path(user.login) %></td>
		<td><%= mail_to user.email%></td>
		<td><%= link_to "Edit", edit_user_path(user)%></td>
		</tr>
  </tbody>
	<% end%>
</table>