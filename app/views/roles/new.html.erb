<% title "New Role" %>

<% form_for [@department, @role] do |f| %>

  <%= render :partial => 'form', :locals => {:f => f} %>
  <p><%= f.submit "Submit" %></p>
<% end %>

<p><%= link_to "Back to List", department_roles_path(@department) %></p>

