<%# we can vertically center text by changing the line-height %>
<ul id="location<%=location.id%>_<%= time_slot_day %>_events" class="events" style="height: 11%;">
  <%= render :partial => "time_slot", :collection => fetch_timeslots(time_slot_day,location) -%>
</ul>
