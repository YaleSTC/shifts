<% for location in loc_group.locations.active %>
	<h3><%= link_to location.name, {:controller => :public_view, :action => 'for_location', :id => location.id }, :locals => {:location => location} %></h3>
	<%= location.description %>
	<%= render :partial => "location", :locals => {:location => location} %>
<% end %>