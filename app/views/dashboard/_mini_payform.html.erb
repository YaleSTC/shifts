<table>
  <tr>
    <th>Date</th>
    <th>Description</th>
    <th>Hours</th>
  </tr>
  <% mini_payform.payform_items.each do |item| %>
    <% if item.active %>
      <tr>
        <td><%= item.date.strftime('%b %d') %></td>
        <td><%= item.description %></td>
        <td><%= item.hours.round.to_s %></td>
      </tr>
    <% end %>
  <% end %>
  <tr>
    <td colspan="2"><strong>Total:</strong></td>
    <td><%= mini_payform.hours.round %></td>
  </tr>
</table>
