<% title @loc_group.name %>
<h4 id="subtitle">Location Group (<%= link_to @loc_group.department.name, @loc_group.department %>)</h4>


<p>
  <%= link_to "Edit", edit_loc_group_path(@loc_group) %> |
  <%= link_to "Destroy", @loc_group, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All Location Groups", department_loc_groups_path(@loc_group.department) %>
</p>

<h2>Locations in this location group</h2>
<% @loc_group.locations.each do |location| %>
<div class="admin_form">
  <fieldset>
    <legend><%= location.name %></legend>
    <%= render :partial => 'locations/summary', :locals => {:location => location} %>
    <p>
      <%= link_to "Show", location %> | 
      <%= link_to "Edit", edit_location_path(location) %> |
      <%= link_to "Destroy", location, :confirm => 'Are you sure?', :method => :delete %>
    </p>
  </fieldset>
</div>
<br />
<% end %>