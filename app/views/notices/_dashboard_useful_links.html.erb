<ul id = "useful_links" >
<% current_department.get_links.each do |link|%>
  <% l = link.content.split("|$|") %>
  <li class = "dashboard_link">
    <% if current_user.is_admin_of?(current_department) %>
        <%= link_to '[x]', { :controller => 'notices', :action => 'destroy', :id => link.id },
                                   :confirm => "Are you sure you want to remove this useful link?",
                                   :method => :delete %>
    <% end %><%= link_to "#{l.first}", "#{l.last}", :popup => true %>
  </li>
<%end%>
<%if current_user.is_admin_of?(current_department) %>
		<li><%= link_to_post_a_link %></li>
<%end%>
</ul>

