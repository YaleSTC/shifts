<% if notice.class.name == "Sticky" %>
  <%= render :partial => 'notices/sticky', :object => notice %>
<% elsif notice.class.name == "Announcement" %>
  <%= render :partial => 'notices/announcement', :object => notice %>
<% end %>

