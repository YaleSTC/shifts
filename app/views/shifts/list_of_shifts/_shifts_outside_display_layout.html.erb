<h4>Shifts outside the display</h4>
<ul>
  <% shifts_outside_display_layout.each do |s| %>
    <li><%= link_to s.short_name, (s.report ? shift_report_path(s) : shift_path(s)) -%></li>
  <% end %>
</ul>