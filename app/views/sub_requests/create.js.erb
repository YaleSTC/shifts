// Correct me if I'm wrong but this file is never called and I think we can delete it
// if @sub_request.errors.empty?
//   redirect_to :action => :show, :id => @sub_request
// else
//   page.replace_html "errors", :partial => 'sub_requests/error_messages'
// end

