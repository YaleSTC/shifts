<% title "New Sub Request" %>
<p>For shift beginning at <%= @sub_request.shift.start.to_s %> and ending at <%= @sub_request.shift.end.to_s %> <p>

<%= render :partial => 'form' %>

<%= link_to 'Back', sub_requests_path %>

